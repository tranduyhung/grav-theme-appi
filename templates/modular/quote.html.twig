<div class="level level-quote">
    <div class="container">
        <div class="row">
            <div class="col-md-6 text-center">
                {{ page.media.images[page.header.image].html()|raw }}
            </div>
            <div class="col-md-6">
                <h2 class="quote-heading">{{ page.header.heading|raw }}</h2>
                <blockquote>
                    {{ page.content|raw }}
                </blockquote>

                <p>{{ page.header.name|raw }}</p>
            </div>
        </div>
    </div>
</div>
